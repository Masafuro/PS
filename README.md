# Python Software Sequencer フレームワーク

## 概要
本プロジェクトは、Pythonを用いてPLC（シーケンサー）のような定周期スキャンと非同期処理を両立させるための制御フレームワークです。メインの制御ロジック（同期）と通信・I/O（非同期）を安全に分離し、データ整合性を自動管理します。

## 実行フェーズの構成
プログラムは役割に応じて `src/` フォルダ直下の4つのディレクトリに分割して配置します。各フォルダ内のスクリプトは**ファイル名の昇順**で実行されます。

> **起動コマンド:** `python main/sequencer.py`

1. **0_setup**: システム起動時に一度だけ実行される初期化フェーズ。
2. **3_keep**: メインループとは独立して非同期に実行される常駐タスク。
3. **1_loop**: 指定されたサイクル時間ごとに繰り返し実行される制御の核。
4. **2_teardown**: プログラム終了時に一度だけ実行される終了処理。

## クイックスタート
1. `src/common/variables.yaml` で変数を定義します。
2. 各フェーズのディレクトリに Python スクリプトを配置します。
3. `python  main/sequencer.py` を実行し、制御を開始します。

## 変数管理の詳細
本フレームワークの最大の特徴である「4ドメイン変数管理システム」については、以下のドキュメントを参照してください。
- [変数仕様詳細 (README_VARIABLES.md)](./README_VARIABLES.md)